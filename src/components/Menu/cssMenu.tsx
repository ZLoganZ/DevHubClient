import styled from 'styled-components';

const StyleProvider = styled.div`
  .sider {
    &::-webkit-scrollbar {
      display: none;
    }
    .ant-menu-item {
      /* color: red; */
    }
  }
`;

export default StyleProvider;
